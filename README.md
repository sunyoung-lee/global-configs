# 🚀 Vibe Coding: Mission Control

> **"문서에 적힌 교훈은 교훈이 아니다. 자동화된 게이트가 되어야 교훈이다."**

이 저장소는 내 모든 프로젝트의 품질을 **물리적으로 강제**하는 중앙 통제실이다.

'안전 제일' 포스터 대신 출입 게이트를 설치한다.
테스트가 통과하지 않으면 커밋이 안 된다. 타협은 없다.

---

## 🧠 철학: 왜 이걸 만들었는가

바이브 코딩의 핵심은 **속도**지만, 속도가 품질을 잡아먹는 순간 모든 것이 무너진다.

| 문제 | 해결 |
|------|------|
| 프로젝트마다 테스트 기준이 다르다 | `Testing_Standard.md` 하나로 전역 통일 |
| AI가 테스트 없이 코드를 밀어넣는다 | `pre-commit` 훅으로 커밋 자체를 차단 |
| 새 프로젝트 시작할 때마다 설정 반복 | `gate-init.sh` 한 줄로 즉시 구축 |
| 설정 파일이 프로젝트마다 중복된다 | 심볼릭 링크로 **단일 원본(Single Source of Truth)** 유지 |

교훈은 기억에서 사라진다. **게이트는 사라지지 않는다.**

---

## 🏛️ 저장소 구조

```text
global-configs/
│
├── scripts/                  # [The Hands] 자동화 스크립트
│   └── gate-init.sh          # 테스트 게이트 + Husky 자동 설치
│
├── templates/                # [The Brain] 표준 지침 & 문서 양식
│   ├── CLAUDE.md             # AI 에이전트 행동 강령 (Master)
│   ├── PRD_Standard.md       # 표준 기획서 양식
│   └── Testing_Standard.md   # 전역 테스트 표준 (UTP v2.0)
│
├── configs/                  # [The Clothes] 공통 설정 파일
│   ├── .prettierrc           # 코드 스타일 통일
│   └── common.gitignore      # 전역 무시 항목
│
├── NAMING_STANDARD.md        # 전역 명명 규칙
├── deploy.sh                 # 마스터 배포 스크립트
└── README.md                 # 지금 읽고 있는 이 파일
```

---

## ⚡ 빠른 시작

### 1. 터미널 단축어

`.zshrc`에 등록된 alias:

```bash
goconfig      # 이 저장소로 즉시 이동
saveconfig    # 변경사항을 커밋하고 GitHub에 푸시
```

### 2. 새 프로젝트에 게이트 설치

프로젝트 루트에서:

```bash
~/github/global-configs/scripts/gate-init.sh
```

이 한 줄이 하는 일:
- `Testing_Standard.md` → 프로젝트 루트에 심볼릭 링크
- Husky가 없으면 자동 설치 + `pre-commit` 훅 생성
- lint-staged가 없으면 자동 설치

### 3. 전체 설정 일괄 배포

```bash
~/github/global-configs/deploy.sh
```

`gate-init.sh` 실행 + 공통 설정 파일 링크를 한 번에 처리한다.

---

## 🔒 게이트가 작동하는 방식

```text
개발자가 코드 수정
        ↓
    git commit
        ↓
  ┌─────────────────┐
  │  pre-commit 훅  │  ← Husky가 자동 실행
  │  (lint-staged)  │
  └────────┬────────┘
           ↓
  변경된 .ts/.tsx 파일에 대해
  vitest related --run 실행
           ↓
    ┌──────┴──────┐
    │             │
  PASS          FAIL
    ↓             ↓
  커밋 성공    커밋 차단 ✋
```

**테스트 없이는 커밋할 수 없다. 이것이 게이트다.**

---

## 🔗 심볼릭 링크 전략

이 저장소의 파일들은 각 프로젝트에 **복사되지 않는다**.
심볼릭 링크로 연결되어 **원본 하나를 수정하면 모든 프로젝트에 즉시 반영**된다.

```bash
# 예시: AppLogic AI 프로젝트
AppLogicAI/Testing_Standard.md → ~/github/global-configs/templates/Testing_Standard.md
```

수정은 항상 이 저장소(`global-configs`)에서. 프로젝트에서 직접 수정하지 않는다.

---

## 📋 현재 관리 중인 프로젝트

| 프로젝트 | 게이트 상태 | 링크된 문서 |
|----------|:-----------:|------------|
| AppLogic AI | ✅ 활성 | Testing_Standard.md |

---

## 🛠️ 관리 워크플로우

### 표준 문서를 수정했을 때

```bash
goconfig                              # 저장소로 이동
# templates/ 안의 파일 수정
saveconfig                            # 커밋 + 푸시
# → 심볼릭 링크로 연결된 모든 프로젝트에 즉시 반영
```

### 새 프로젝트를 시작할 때

```bash
cd ~/새프로젝트
~/github/global-configs/scripts/gate-init.sh  # 게이트 설치
# 코딩 시작. 테스트 없이는 커밋 불가.
```

---

## 🧭 Naming Standard 적용

전역 명명 규칙은 `NAMING_STANDARD.md`를 기준으로 적용한다.

### 현재 상태 진단 (인벤토리)

```bash
~/github/global-configs/scripts/naming-inventory.sh ~/github
```

### 로컬 저장소명 통일 (kebab-case)

```bash
# dry-run
~/github/global-configs/scripts/rename-local-repos.sh --root ~/github

# apply
~/github/global-configs/scripts/rename-local-repos.sh --apply --root ~/github
```

### GitHub 원격 저장소명 통일

```bash
# dry-run
~/github/global-configs/scripts/rename-github-repos.sh --root ~/github

# apply
~/github/global-configs/scripts/rename-github-repos.sh --apply --root ~/github
```

### 전체 repo에 네이밍 게이트 배포

```bash
~/github/global-configs/scripts/install-naming-gate-all.sh --root ~/github
```

### 코드 네이밍 점검

```bash
cd ~/github/your-repo
bash scripts/validate-repo-name.sh
node scripts/naming-check.mjs
```

### 리포트 생성

```bash
~/github/global-configs/scripts/generate-naming-reports.sh ~/github
```

---

> **"Move fast, but never break the gate."**
